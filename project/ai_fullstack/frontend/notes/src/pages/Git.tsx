import React from 'react'
import { useGitStore } from '@/store/useGitStore'

const  Git: React.FC = () => {
    const {} = useGitStore();

    return (
        <div>

        </div>
    )
}

export default Git